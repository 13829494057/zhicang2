# 部署到知乎 AiWorks（或其他平台）

目标：让**任何人**输入知乎账号登录后，看到自己的收藏夹考古报告。

## 为什么必须部署到公网

OAuth 登录成功后，知乎会让用户浏览器跳转回 `redirect_uri`。这个地址：

1. 必须与赛事页面登记值**完全一致**（协议、域名、路径、参数）；
2. 需要公网可达 —— `http://127.0.0.1` 只能自测，知乎无法验证归属，**陌生人走不通**。

所以顺序是：**先部署拿到 HTTPS 域名 → 再去赛事页面填回调地址 → 最后联调**。
官方未说明回调地址创建后能否修改，务必一次填对。

## 一、部署步骤（AiWorks）

本项目架构正好命中 AiWorks 最轻的一档：前端 + Node 后端，进程内存 Map，**零数据库写入**。

1. 把整个 `fav-archaeology/` 目录推上去（`.env` 不要提交，靠平台环境变量注入）。
2. 启动命令：`npm start`（等价 `node src/server.js`）。
3. 在平台的**服务端环境变量**里配置（不要写进代码）：

   | 变量 | 值 |
   |---|---|
   | `ZHIHU_APP_ID` | 528 |
   | `ZHIHU_APP_KEY` | 赛事页面分配的 App Key |
   | `ZHIHU_ACCESS_SECRET` | 团队任一成员的开放平台 Secret |
   | `ZHIHU_REDIRECT_URI` | `https://你的域名/auth/zhihu/callback` |
   | `SESSION_SECRET` | 任意随机串 |

   **不要**设置 `ALLOW_DEV_LOGIN`（那是本地预览用的后门）。

4. 平台会注入 `PORT`，服务已监听 `0.0.0.0:$PORT`，无需改动。

## 二、开工前 30 分钟必须验的三件事（文档第三十四节）

验不过就切 CloudBase，不要恋战：

- [ ] 能否配置服务端环境变量（App Key 必须放这里）
- [ ] 是否默认 HTTPS
- [ ] 进程是否常驻（我们的会话缓存在进程内存里）

## 三、多实例部署的注意事项

当前会话（OAuth token + 扫描快照）存在**进程内存 Map**。若平台是多实例或冷启动回收：

- 负载均衡会把第二个请求打到另一个实例，表现为**随机掉线**；
- 冷启动会丢失所有会话。

**单实例常驻**部署可直接用。需要多实例时，把 `src/lib/session.js` 的 Map 换成
共享存储（Redis 等），扫描快照可作为纯缓存丢失即重算。

## 四、上线后必验的清单

用**别人的账号 + 无痕窗口**跑，不能只用自己的：

- [ ] 陌生人从点登录到看到报告，全程无人指导能走通
- [ ] 未实名账号：失败页写出「需绑手机号并实名」三条自查，不显示「系统错误」
- [ ] 空收藏夹账号：出空态页，不白屏
- [ ] 薄数据账号（收藏 <20 条或跨度 <90 天）：走轻量档，屏一正确关闭
- [ ] token 失效后：走重新授权，**绝不显示开发者本人的收藏夹**
- [ ] 换 A 账号登录看到 A 的数据，退出换 B 看到 B 的数据（会话不串号）
- [ ] 额度归属：让队友在线上扫一次，查你自己账号 `zhihu-cli quota` 的 user_data 确实减少

## 五、额度是唯一硬天花板

`user_data` 实测日额度 **1,000 次**，所有访客共用。已内置三道闸（`src/lib/ratelimit.js`）：

- **令牌桶**：限每秒发起的扫描数（桶容量 3，每秒补 1）
- **并发闸**：同时深挖 ≤ 3
- **三级熔断**（按 quota 剩余比例自动切换）：
  - `> 30%` full：L1 + L2 全开
  - `10–30%` l1only：关闭深挖，只留速览
  - `3–10%` throttle：限流
  - `< 3%` degraded：登录入口降级，提示「今日名额已满」

L1 速览每人只花 **1 次**请求，L2 深挖 ≤ 40 次。默认档是 L1，深挖需用户主动点。
按 1,000 额度算，L1 约可服务 800+ 人/天。

**上线前建议先提交提额申请**（文档第三十七节），把 user_data 提到 10,000。

## 六、演示话术

评委问「额度打光怎么办」，不用解释 —— 页面角标实时显示剩余额度（来自 quota 接口的
真实 `RemainingQuota`），并可现场演示熔断降级。这是官方提交前检查第 6 条
「额度耗尽有真实降级提示」的直接达标证据。
